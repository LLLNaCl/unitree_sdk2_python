# 宇树机器狗B2环境变量配置文档

## 一、环境变量概览

以下是机器狗B2代码封装方案中需要配置的所有环境变量，按照功能模块分类：

### 1. 基础设备配置
- `ROBOT_DEVICE_ID`: 机器狗设备唯一标识符
- `ROBOT_SECRET_KEY`: 设备认证密钥
- `ROBOT_MODEL`: 机器人型号，默认为"B2"
- `ROBOT_NAME`: 机器人名称，用于显示

### 2. MQTT通信配置
- `MQTT_BROKER_HOST`: MQTT服务器地址
- `MQTT_BROKER_PORT`: MQTT服务器端口
- `MQTT_USERNAME`: MQTT认证用户名
- `MQTT_PASSWORD`: MQTT认证密码
- `MQTT_KEEPALIVE`: MQTT心跳间隔（秒）
- `MQTT_QOS`: MQTT消息服务质量
- `MQTT_CONNECT_TIMEOUT`: MQTT连接超时（秒）

### 3. 网络配置
- `NETWORK_PREFERENCE`: 网络优先级，可选"4G"或"WiFi"
- `WIFI_SSID`: WiFi网络名称
- `WIFI_PASSWORD`: WiFi网络密码
- `WWAN_INTERFACE`: 4G模块网络接口
- `MAX_RECONNECT_ATTEMPTS`: 最大重连尝试次数
- `RECONNECT_DELAY`: 重连延迟（秒）

### 4. 视频流配置
- `RTSP_URL`: RTSP视频流URL
- `VIDEO_WIDTH`: 视频宽度
- `VIDEO_HEIGHT`: 视频高度
- `VIDEO_FPS`: 视频帧率
- `VIDEO_BITRATE`: 视频比特率
- `VIDEO_PRESET`: 视频编码预设
- `FFMPEG_PATH`: FFmpeg可执行文件路径

### 5. 日志配置
- `LOG_LEVEL`: 日志级别，可选"DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"
- `LOG_FILE`: 日志文件路径
- `LOG_MAX_SIZE`: 日志文件最大大小（MB）
- `LOG_BACKUP_COUNT`: 日志备份文件数量

### 6. 系统配置
- `HEARTBEAT_INTERVAL`: 心跳包发送间隔（秒）
- `SENSOR_UPDATE_INTERVAL`: 传感器数据更新间隔（毫秒）
- `COMMAND_TIMEOUT`: 指令超时时间（秒）
- `MAX_COMMAND_QUEUE`: 最大指令队列长度
- `SYSTEM_CHECK_INTERVAL`: 系统状态检查间隔（秒）

### 7. 导航与定位配置
- `GPS_ENABLED`: 是否启用GPS
- `ODOMETRY_ENABLED`: 是否启用里程计
- `LOCALIZATION_METHOD`: 定位方法，可选"GPS", "SLAM", "FUSION"
- `MAP_UPDATE_INTERVAL`: 地图更新间隔（秒）
- `NAVIGATION_TIMEOUT`: 导航超时时间（秒）

### 8. 安全配置
- `ENCRYPTION_ENABLED`: 是否启用数据加密
- `ENCRYPTION_KEY`: 加密密钥
- `SECURE_COMMUNICATION`: 是否启用安全通信（TLS/SSL）
- `CERTIFICATE_PATH`: 证书文件路径
- `PRIVATE_KEY_PATH`: 私钥文件路径

## 二、Bash环境变量配置语句

### 1. 基础设备配置
```bash
export ROBOT_DEVICE_ID="b2_robot_001"
export ROBOT_SECRET_KEY="your_secure_secret_key_here"
export ROBOT_MODEL="B2"
export ROBOT_NAME="宇树机器狗B2"
```

### 2. MQTT通信配置
```bash
export MQTT_BROKER_HOST="mqtt.example.com"
export MQTT_BROKER_PORT="1883"
export MQTT_USERNAME="robot_user"
export MQTT_PASSWORD="secure_mqtt_password"
export MQTT_KEEPALIVE="60"
export MQTT_QOS="1"
export MQTT_CONNECT_TIMEOUT="10"
```

### 3. 网络配置
```bash
export NETWORK_PREFERENCE="4G"
export WIFI_SSID="Your_WiFi_Name"
export WIFI_PASSWORD="Your_WiFi_Password"
export WWAN_INTERFACE="wwan0"
export MAX_RECONNECT_ATTEMPTS="5"
export RECONNECT_DELAY="3"
```

### 4. 视频流配置
```bash
export RTSP_URL="rtsp://example.com:554/stream/b2_robot_001"
export VIDEO_WIDTH="640"
export VIDEO_HEIGHT="360"
export VIDEO_FPS="30"
export VIDEO_BITRATE="1000000"
export VIDEO_PRESET="ultrafast"
export FFMPEG_PATH="/usr/bin/ffmpeg"
```

### 5. 日志配置
```bash
export LOG_LEVEL="INFO"
export LOG_FILE="/var/log/robot/b2_robot.log"
export LOG_MAX_SIZE="10"
export LOG_BACKUP_COUNT="5"
```

### 6. 系统配置
```bash
export HEARTBEAT_INTERVAL="5"
export SENSOR_UPDATE_INTERVAL="100"
export COMMAND_TIMEOUT="10"
export MAX_COMMAND_QUEUE="10"
export SYSTEM_CHECK_INTERVAL="30"
```

### 7. 导航与定位配置
```bash
export GPS_ENABLED="true"
export ODOMETRY_ENABLED="true"
export LOCALIZATION_METHOD="FUSION"
export MAP_UPDATE_INTERVAL="2"
export NAVIGATION_TIMEOUT="30"
```

### 8. 安全配置
```bash
export ENCRYPTION_ENABLED="true"
export ENCRYPTION_KEY="your_32_byte_encryption_key_here"
export SECURE_COMMUNICATION="true"
export CERTIFICATE_PATH="/etc/robot/certs/client.crt"
export PRIVATE_KEY_PATH="/etc/robot/certs/client.key"
```

## 三、环境变量配置文件

### 1. 创建环境变量配置文件

创建一个名为`robot_env.sh`的配置文件：

```bash
cat > /etc/robot/robot_env.sh << 'EOF'
#!/bin/bash

# 基础设备配置
export ROBOT_DEVICE_ID="b2_robot_001"
export ROBOT_SECRET_KEY="your_secure_secret_key_here"
export ROBOT_MODEL="B2"
export ROBOT_NAME="宇树机器狗B2"

# MQTT通信配置
export MQTT_BROKER_HOST="mqtt.example.com"
export MQTT_BROKER_PORT="8883"
export MQTT_USERNAME="robot_user"
export MQTT_PASSWORD="secure_mqtt_password"
export MQTT_KEEPALIVE="60"
export MQTT_QOS="1"
export MQTT_CONNECT_TIMEOUT="10"

# 网络配置
export NETWORK_PREFERENCE="4G"
export WIFI_SSID="Your_WiFi_Name"
export WIFI_PASSWORD="Your_WiFi_Password"
export WWAN_INTERFACE="wwan0"
export MAX_RECONNECT_ATTEMPTS="5"
export RECONNECT_DELAY="3"

# 视频流配置
export RTSP_URL="rtsp://example.com:554/stream/b2_robot_001"
export VIDEO_WIDTH="640"
export VIDEO_HEIGHT="360"
export VIDEO_FPS="30"
export VIDEO_BITRATE="1000000"
export VIDEO_PRESET="ultrafast"
export FFMPEG_PATH="/usr/bin/ffmpeg"

# 日志配置
export LOG_LEVEL="INFO"
export LOG_FILE="/var/log/robot/b2_robot.log"
export LOG_MAX_SIZE="10"
export LOG_BACKUP_COUNT="5"

# 系统配置
export HEARTBEAT_INTERVAL="5"
export SENSOR_UPDATE_INTERVAL="100"
export COMMAND_TIMEOUT="10"
export MAX_COMMAND_QUEUE="10"
export SYSTEM_CHECK_INTERVAL="30"

# 导航与定位配置
export GPS_ENABLED="true"
export ODOMETRY_ENABLED="true"
export LOCALIZATION_METHOD="FUSION"
export MAP_UPDATE_INTERVAL="2"
export NAVIGATION_TIMEOUT="30"

# 安全配置
export ENCRYPTION_ENABLED="true"
export ENCRYPTION_KEY="your_32_byte_encryption_key_here"
export SECURE_COMMUNICATION="true"
export CERTIFICATE_PATH="/etc/robot/certs/client.crt"
export PRIVATE_KEY_PATH="/etc/robot/certs/client.key"
EOF
```

### 2. 设置文件权限

```bash
chmod 600 /etc/robot/robot_env.sh
chown robot:robot /etc/robot/robot_env.sh
```

### 3. 自动加载环境变量

在`/etc/profile.d/`目录下创建一个脚本，确保环境变量在系统启动时自动加载：

```bash
cat > /etc/profile.d/robot_env.sh << 'EOF'
#!/bin/bash
if [ -f /etc/robot/robot_env.sh ]; then
    source /etc/robot/robot_env.sh
fi
EOF

chmod 755 /etc/profile.d/robot_env.sh
```

### 4. 为服务配置环境变量

对于systemd服务，创建服务文件时需要指定环境变量文件：

```bash
cat > /etc/systemd/system/robot.service << 'EOF'
[Unit]
Description=Unitree B2 Robot Service
After=network.target

[Service]
User=robot
Group=robot
EnvironmentFile=/etc/robot/robot_env.sh
ExecStart=/usr/bin/python3 /opt/robot/main.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF
```

## 四、环境变量使用示例

在Python代码中使用环境变量的示例：

```python
import os
import logging
from dotenv import load_dotenv

# 加载环境变量
load_dotenv('/etc/robot/robot_env.sh')

# 获取设备ID
device_id = os.getenv('ROBOT_DEVICE_ID')

# 配置日志
logging.basicConfig(
    level=os.getenv('LOG_LEVEL', 'INFO'),
    filename=os.getenv('LOG_FILE', '/var/log/robot/b2_robot.log'),
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)

# MQTT配置
mqtt_config = {
    'host': os.getenv('MQTT_BROKER_HOST'),
    'port': int(os.getenv('MQTT_BROKER_PORT', 8883)),
    'username': os.getenv('MQTT_USERNAME'),
    'password': os.getenv('MQTT_PASSWORD'),
    'keepalive': int(os.getenv('MQTT_KEEPALIVE', 60)),
    'qos': int(os.getenv('MQTT_QOS', 1))
}
```